workflows:
  android-workflow:
    name: Android Build
    environment:
      java: 17

    scripts:
      - name: Give gradle permission
        script: chmod +x gradlew

      - name: Build APK
        script: |
          ./gradlew assembleDebug

    artifacts:
      - app/build/outputs/**/*.apk
